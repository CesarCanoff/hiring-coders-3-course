const assert = require('assert');
const { Given, When, Then } = require('cucumber');

Given('I\m have \{string} clientes in my database', function (string) {
  return 'pending';
})

When('I access the home webpage', function () {
  return 'pending';
})

Then('see the list of \{string} itens', function (string) {
  return 'pending';
})